---
import { type LocalesValues } from "intlayer";
import LanguageSwitcher from "@/components/LanguageSwitcher.astro";

interface Props {
  locale: LocalesValues;
  content: Record<string, string>;
  currentPath: string;
}

const { locale, content: nav, currentPath } = Astro.props;
---

<nav
  class="fixed top-0 right-0 z-40 hidden items-center gap-1 px-8 py-4 md:flex"
>
  <a
    href={nav.link1Href}
    class="rounded-md px-3 py-2 text-sm font-medium text-gray-400 transition hover:bg-white/10 hover:text-white"
    >{nav.link1Label}</a
  >
  <a
    href={nav.link2Href}
    class="rounded-md px-3 py-2 text-sm font-medium text-gray-400 transition hover:bg-white/10 hover:text-white"
    >{nav.link2Label}</a
  >
  <a
    href={nav.link3Href}
    class="rounded-md px-3 py-2 text-sm font-medium text-gray-400 transition hover:bg-white/10 hover:text-white"
    >{nav.link3Label}</a
  >
  <a
    href={nav.link4Href}
    class="rounded-md px-3 py-2 text-sm font-medium text-gray-400 transition hover:bg-white/10 hover:text-white"
    >{nav.link4Label}</a
  >
  <a
    href={nav.link5Href}
    class="rounded-md px-3 py-2 text-sm font-medium text-gray-400 transition hover:bg-white/10 hover:text-white"
    >{nav.link5Label}</a
  >
  <LanguageSwitcher locale={locale} currentPath={currentPath} />
</nav>
